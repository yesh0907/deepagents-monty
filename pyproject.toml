[project]
name = "deepagents-monty"
version = "0.2.0"
description = "Monty-backed Python REPL middleware for Deep Agents, with a shared filesystem between the sandbox and the agent's file tools."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Yesh Chandiramani" }]
keywords = [
    "llm",
    "agents",
    "langchain",
    "langgraph",
    "deepagents",
    "pydantic",
    "monty",
    "sandbox",
    "code-execution",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "pydantic-monty>=0.0.17",
    "deepagents>=0.5.3",
    "langchain>=1.0.0",
    "langchain-core>=1.0.0",
    "langgraph>=1.0.0",
]

[project.optional-dependencies]
evals = [
    "langchain-openai>=0.3.26",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "twine>=6.0",
    "pyright>=1.1.390",
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.6",
]

[project.urls]
Repository = "https://github.com/yesh0907/deepagents-monty"
Issues = "https://github.com/yesh0907/deepagents-monty/issues"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = "src"
module-name = "deepagents_monty"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
]
ignore = [
    "E501",  # line-too-long handled by formatter
]
