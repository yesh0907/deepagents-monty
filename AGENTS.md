- use the langsmith cli to fetch data from langsmith
- download traces from langsmith into the .langsmith directory at the root of the repo (create the directory if it doesn't exist)

## Agent skills

### Issue tracker

Issues and PRDs are tracked in GitHub Issues for `yesh0907/deepagents-monty`. See `docs/agents/issue-tracker.md`.

### Triage labels

Triage labels use the default mattpocock/skills vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single-context domain documentation layout. See `docs/agents/domain.md`.
